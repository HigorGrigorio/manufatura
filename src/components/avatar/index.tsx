import { AvatarFallback } from './components/avatar-fallback.tsx';
import { AvatarImage } from '@/components/avatar/components/avatar-image.tsx';
import { AvatarRoot } from './components/avatar-root.tsx';

export const Avatar = {
    Fallback: AvatarFallback,
    Image: AvatarImage,
    Root: AvatarRoot,
};
